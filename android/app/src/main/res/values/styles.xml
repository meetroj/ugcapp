<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

    <!--
      Theme used only while the process starts and the JS bundle loads. Without
      it the app opens on a blank white window before React can paint anything.
      MainActivity switches back to AppTheme in onCreate.

      Pre-Android-12 devices simply draw the layer-list as the window
      background; values-v31 overrides this with the SplashScreen API.
    -->
    <style name="SplashTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

</resources>
